class GetProductsByPageQuery {
    constructor(
        page,
        pageSize
    ) {
        this.page = page;
        this.pageSize = pageSize;
    }
}

module.exports = { GetProductsByPageQuery } 